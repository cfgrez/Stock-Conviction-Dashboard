{
  "name": "stock-conviction-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard bursátil de tres filtros para Cloudflare Pages Functions.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name stock-conviction-dashboard",
    "check": "node --check public/app.js && node --check functions/api/analyze.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
